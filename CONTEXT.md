# CONTEXT.md

**프로젝트**: moyemap  
**서비스 URL**: https://moyemap.lomeone.com/  
**마지막 업데이트**: 2026-05-14

## 한 줄 정의

모여맵은 서울 주요 상권의 소셜파티, 혼술바, 게스트하우스 파티, 네트워킹 모임을 지도와 목록으로 비교하고 외부 예약 링크로 전환시키는 정보 탐색 서비스다.

## 제품 목표

### 해결하려는 문제

2030 1인 가구, 직장인, 국내 여행객은 새로운 사람을 만날 수 있는 모임 정보를 여러 플랫폼과 SNS에서 따로 찾아야 한다. 모여맵은 위치, 가격, 분위기, 제공 내역을 한 화면에서 비교하게 해 탐색 시간을 줄이고 예약 전환까지 이어지게 한다.

### MVP 가설

서울의 2030 사용자는 흩어진 소셜 파티, 혼술바, 게스트하우스 파티 정보를 지도에서 보고 가격과 위치를 비교한 뒤 외부 예약 페이지로 이동할 것이다.

### 초기 타겟 지역

- 홍대/연남
- 강남
- 이태원
- 성수

### MVP 범위

- 포함: 지도 기반 탐색, 리스트/카드 비교, 카테고리/가격/지역 필터, 상세 페이지, 외부 예약 링크
- 제외: 회원가입/로그인, 자체 결제, 호스트 백오피스, 리뷰 작성, 평점 신뢰도 검증, 실시간 재고/좌석 관리

## 현재 구현 상태

### 프론트엔드

- Next.js 16 App Router, React 19, TypeScript 기반
- Tailwind CSS v4 사용, CSS 변수 기반 라이트/다크 테마 제공
- `src/app/layout.tsx`에서 Naver Cloud Map SDK 스크립트를 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID`로 로드
- 메인 화면은 `src/app/page.tsx`에서 지도, 필터, 데스크톱 사이드바, 모바일 지도/목록 전환, 모바일 바텀 시트를 조합
- 상세 화면은 `src/app/party/[id]/page.tsx`와 `PartyDetailContent.tsx`에서 정적 파라미터, 메타데이터, 이미지 슬라이더, 위치/소개/제공 내역/리뷰/CTA를 제공
- 목 데이터는 `src/data/mockParties.ts`에 있으며 타입은 `src/types/party.ts`에서 관리
- `robots.ts`, `sitemap.ts`가 있으나 현재 `BASE_URL`은 `https://moyemap.com`으로 되어 있어 실제 배포 도메인과 다르다

### 핵심 컴포넌트

- `Header`: 브랜드, 서비스 카피, 테마 토글
- `QuickFilters`: 카테고리, 가격, 지역 칩 필터
- `PartyListPanel`: 모임 개수, 정렬, 선택 카드 자동 스크롤
- `PartyCard`: 이미지, 카테고리, 가격, 장소, 태그, 상세 링크
- `NaverMap`: 네이버 지도 초기화, 카테고리별 가격 마커, 정보창, 선택 동기화, 이 지역 다시 검색 버튼
- `ThemeProvider`: 로컬스토리지 기반 테마 저장 및 `dark` 클래스 토글

### 데이터 모델

현재 `Party`는 다음 필드를 가진다.

- 기본 정보: `id`, `title`, `category`, `description`, `tags`
- 위치: `location.name`, `location.address`, `lat`, `lng`, `region`
- 가격/이미지: `price`, `imageUrl`, `images`
- 상세 정보: `amenities`, `sourceUrl`
- 리뷰: `reviews`, `averageRating`

운영 사이트 HTML에서는 일정, 시간, 연령대, 성비 같은 정보가 노출되는 흔적이 있다. 로컬 타입에는 아직 해당 필드가 없으므로, 다음 데이터 모델 정리 때 배포본과 저장소의 스키마 차이를 맞춰야 한다.

## 주요 사용자 흐름

1. 사용자는 첫 화면에서 서울 중심 지도와 모임 목록을 본다.
2. 카테고리, 가격대, 지역 필터로 탐색 범위를 좁힌다.
3. 지도 마커 또는 목록 카드를 선택해 위치와 가격을 비교한다.
4. 상세 페이지에서 이미지, 위치, 제공 내역, 리뷰를 확인한다.
5. `예약하기` CTA를 눌러 외부 플랫폼으로 이동한다.

## UI/UX 개선 방향

### 1. 탐색의 첫 판단값 강화

현재 카드는 이미지, 제목, 위치, 가격 중심이라 빠르게 비교하기 좋지만 모임 결정에 중요한 일정/시간/나이대/성비/잔여 자리 정보가 약하다. 카드와 마커 정보창에 날짜, 시작 시간, 참가 조건, 마감 임박 여부를 우선 노출하면 사용자가 상세 페이지로 들어가기 전 판단 비용을 줄일 수 있다.

### 2. 필터 체계 재정리

카테고리, 가격, 지역은 잘 맞지만 PRD의 일정 필터가 로컬 코드에서는 빠져 있다. 우선순위는 `오늘`, `내일`, `이번 주말` 일정 필터를 복구하고, 이후에는 `혼자 가기 좋음`, `대화 중심`, `음주 포함`, `초보 환영` 같은 의도 기반 필터를 추가하는 쪽이 전환에 더 가깝다.

### 3. 지도와 목록의 상호작용 명확화

선택 동기화는 구현되어 있지만, 사용자가 "지금 어떤 모임을 보고 있는지"를 더 강하게 느끼게 해야 한다. 선택된 카드의 시각 강조, 지도 마커의 z-index/확대, 바텀 시트에서 선택 항목 상단 고정, 정보창 CTA를 통일하면 지도 앱다운 감각이 좋아진다.

### 4. 모바일 바텀 시트 고도화

모바일에서는 지도와 목록이 핵심이다. 현재는 접기/펼치기 중심이라 좋지만, 반쯤 열린 상태에서 상위 2~3개 추천 카드가 보이면 지도 탐색과 비교가 더 자연스럽다. 바텀 시트에는 필터 요약, 결과 개수, 정렬 옵션, 선택 카드 프리뷰가 함께 있어야 한다.

### 5. 신뢰와 전환 요소 보강

더미 리뷰는 데모에 좋지만 실제 서비스에서는 리뷰 신뢰도가 민감하다. MVP에서는 자체 리뷰보다 `외부 출처`, `포함 내역`, `운영 시간`, `환불/문의 방식`, `예약처`를 명확히 보여주는 편이 안전하다. CTA에는 `예약하기`만 두기보다 `예약처로 이동`, `문의하기` 등 실제 source 성격에 맞춘 라벨이 필요하다.

### 6. 시각 디자인 톤

현재 다크 테마와 코랄 포인트는 소셜 서비스에 어울린다. 다만 지도 서비스는 정보 밀도가 높아야 하므로 카드 둥근 정도, 그림자, 태그 수, 색상 수를 조금 절제하고 가격/시간/위치의 계층을 더 분명히 하는 것이 좋다. 라이트 모드는 지도 가독성에 유리하므로 기본값을 사용자 시스템 설정에 맡기되, 지도 위 오버레이는 라이트/다크 모두 대비를 충분히 확보해야 한다.

### 7. SEO와 공유 경험

상세 페이지 메타데이터는 있다. 다음으로는 실제 도메인 기준 sitemap/robots 수정, 상세 페이지 Open Graph 이미지/제목/설명 추가, 카카오톡 공유 시 보이는 문구 최적화가 필요하다.

## 다음 우선순위

1. 로컬 코드와 배포본의 데이터 모델 차이 정리: 날짜, 시간, 연령대, 성비 필드 포함 여부 결정
2. 일정 필터 추가 및 `PartyListPanel` 정렬 기준을 날짜/추천/가격으로 재정의
3. 실제 도메인 기준 `robots.ts`, `sitemap.ts`, 메타데이터 정리
4. 지도 이동 후 `이 지역에서 다시 검색`이 실제 bounds 기반 필터/API 요청으로 이어지게 구현
5. CTA 클릭 이벤트 로깅 구조 설계
6. 더미 데이터 출처를 실제 크롤링/제휴 데이터로 대체하기 위한 API DTO 초안 작성

## 기술 메모

- 패키지 매니저는 Yarn을 사용한다.
- 주요 명령어는 `yarn dev`, `yarn build`, `yarn lint`다.
- 모든 사용자 대면 문구는 한국어로 작성한다.
- 통화 표기는 `₩{value.toLocaleString()}` 형식을 유지한다.
- 지도 SDK가 없거나 환경변수가 없을 때의 빈 상태/로딩 상태를 반드시 고려한다.
- 네이버 지도 타입은 `src/types/naver-maps.d.ts`에 최소 타입으로 선언되어 있다.
- 목 데이터가 커질 경우 `mockParties.ts` 단일 파일보다 fixtures/data layer를 분리하는 편이 좋다.

## 성공 지표

- DAU / MAU
- 지도 이동, 마커 클릭, 카드 클릭 등 탐색 활성도
- 상세 페이지 진입률
- 외부 예약 CTA 클릭률
- 지역/카테고리/가격대별 검색 비율
- 모바일 바텀 시트 열림률과 목록 스크롤 깊이
